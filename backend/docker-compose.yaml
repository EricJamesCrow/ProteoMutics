services:
  app:
    build: .
    container_name: proteomutics-api
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-include *
    # command: python3.10 manage.py runserver 0.0.0.0:8000 --noreload --nothreading
    ports:
      - "8000:8000"
    volumes:
      - .:/app